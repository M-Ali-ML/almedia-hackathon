---
name: log-code-changes
description: >-
  Append a short changelog entry after every code edit. Use whenever the agent
  creates, modifies, deletes, or renames source files, configs, or scripts.
  Canonical rules live in AGENTS.md (shared by all coding agents).
---

# Log Code Changes

Follow the canonical instructions in [AGENTS.md](../../../AGENTS.md).

**Summary:** after each coherent batch of code edits, append one or two sentences to `logs/agent-changes.log` (create `logs/` if needed). Never overwrite the log; never log the log file itself.
